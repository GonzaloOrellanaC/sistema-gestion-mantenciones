// DynamicListModal removed - dynamic_list is now handled inline in FormRenderer
// Kept as a minimal stub to avoid import errors in case of leftover references.
const DynamicListModalStub: any = null;
export default DynamicListModalStub;
