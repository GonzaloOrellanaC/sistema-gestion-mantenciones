// Compatibility wrapper: re-export `PartSelector` as `ComponentSelector` to keep older imports working.
import PartSelector from './PartSelector.widget';

export const ComponentSelector = PartSelector;
export default PartSelector;
